/*======================== typing animasi======================*/
var typed = new Typed(".typing",{
    strings:["","islam nusantara university"],
    typeSpeed:100,
    BackSpeed:60,
    loop:true

})